@model IEnumerable<WimbledonWines.Models.Winery>

@{
    ViewBag.Title = "All Wineries";
}

 @Html.Partial("_confirmmsg") <!--A prartial view to inform user of sucssful actions using bootstrp alters -->

 
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Country)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Country)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>

<div class="text-center">
    
        @Html.ActionLink("Create New winery", "Create", null, new { @class = "btn btn-default " })
     

    @Html.ActionLink("Back To Admins Homepage", "Index", "Manage", null, new { @class = "btn btn-btn btn-default btn-lg" })
</div>
